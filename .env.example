# Development settings — DO NOT commit your real .env with secrets.
# Copy this file to .env and fill in your Atlas password and optional DB name.

PORT=4000
# Example (replace <db_password> and <dbname>):
# MONGO_URI=mongodb+srv://<username>:<db_password>@big-bull.vv33cks.mongodb.net/<dbname>?retryWrites=true&w=majority
# If your password has special characters, URL-encode it (see README or use node's encodeURIComponent).

