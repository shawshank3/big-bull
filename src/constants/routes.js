export const ROUTES = Object.freeze({
  ROOT: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  HOLDINGS: '/holdings',
  PORTFOLIO: '/portfolio',
});

export default ROUTES;
